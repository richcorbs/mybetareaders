<%= form_for(@criterium) do |f| %>
  <% if @criterium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@criterium.errors.count, "error") %> prohibited this criterium from being saved:</h2>

      <ul>
      <% @criterium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :criterium, "Criterium" -%> <%= f.text_field :criterium -%></p>
  <p><%= f.label :description -%> <%= f.text_field :description -%></p>
  <p><%= f.label :fiction, "Fiction?" -%> <%= f.check_box :fiction -%></p>
  <p><%= f.label :nonfiction, "Non-Fiction?" -%> <%= f.check_box :nonfiction -%></p>
  <%= f.submit nil, :class => 'btn btn-success' %>
<% end %>
